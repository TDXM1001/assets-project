# Design: 资产管理系统前端页面与路由骨架清单

Generated by /office-hours on 2026-03-24
Branch: main
Repo: assets-project
Status: DRAFT
Mode: Builder
Supersedes: 资产管理系统-后端代码骨架清单.md

## 目标

这份文档把资产管理系统继续推进到前端实施层，重点回答四件事：

1. 资产模块在当前 `art-design-pro` 里应该落到哪些目录
2. 页面、路由、API 文件应该怎么拆
3. 哪些页面应该优先做列表页骨架，哪些要从一开始就按流程页来设计
4. 在“不使用代码生成器”的前提下，前端怎样和后端手写接口保持稳定对齐

## 项目基线

当前前端目录可以确认：

1. 路由模块在 `art-design-pro/src/router/modules`
2. 页面目录在 `art-design-pro/src/views`
3. API 按业务域拆在 `art-design-pro/src/api/*`
4. 权限指令使用 `v-auth`，实现位于 `art-design-pro/src/directives/business/permi.ts`

现状里已经存在：

1. `art-design-pro/src/views/asset`
2. `art-design-pro/src/views/asset/ledger`

但目前 `asset/ledger` 还是空壳，没有形成稳定页面结构。

所以这一轮的建议是：

1. 不沿着 `ledger` 继续发散
2. 统一收敛到 `src/views/asset/*`
3. 让路由、页面、API、权限命名都和后端 `/asset/**` 完整对齐

## 当前建议

### 建议 1：单独新增 `router/modules/asset.ts`

当前 `src/router/modules/index.ts` 只挂了：

1. `dashboard`
2. `system`
3. `result`
4. `exception`

资产模块不应该继续塞进 `system.ts`，更稳的做法是新增：

1. `art-design-pro/src/router/modules/asset.ts`

然后在：

1. `art-design-pro/src/router/modules/index.ts`

里统一导出。

### 建议 2：页面全部收敛在 `src/views/asset`

建议结构如下：

1. `src/views/asset/dashboard`
2. `src/views/asset/category`
3. `src/views/asset/location`
4. `src/views/asset/info`
5. `src/views/asset/order`
6. `src/views/asset/inventory`
7. `src/views/asset/event`

不建议再保留一个语义模糊的：

1. `src/views/asset/ledger`

如果你后面确实只想保留“资产台账”这个名字，那么也建议直接用：

1. `src/views/asset/info`

这样和后端 `/asset/info/*`、菜单 `asset:info:*`、文档里的“资产台账”都一致。

### 建议 3：API 按领域拆文件，不要堆成一个大 `asset.ts`

建议新增：

1. `src/api/asset/category.ts`
2. `src/api/asset/location.ts`
3. `src/api/asset/info.ts`
4. `src/api/asset/order.ts`
5. `src/api/asset/inventory.ts`
6. `src/api/asset/event.ts`
7. `src/api/asset/dashboard.ts`
8. `src/api/asset/attachment.ts`

### 建议 4：页面模块按“列表页骨架”和“流程页骨架”两类写

分类、位置、台账这类页面，适合复用系统管理页面的“搜索栏 + 表格 + 弹窗”模式。

单据、盘点这类页面，从第一版开始就要接受它不是普通 CRUD 页面，而是：

1. 列表页
2. 详情页/抽屉
3. 动作弹窗
4. 状态条
5. 流程按钮

## 推荐目录结构

### 路由

建议新增：

1. `art-design-pro/src/router/modules/asset.ts`

### API

建议新增目录：

1. `art-design-pro/src/api/asset`

目录下建议新增：

1. `category.ts`
2. `location.ts`
3. `info.ts`
4. `order.ts`
5. `inventory.ts`
6. `event.ts`
7. `dashboard.ts`
8. `attachment.ts`

### 页面

建议新增目录：

1. `art-design-pro/src/views/asset/dashboard`
2. `art-design-pro/src/views/asset/category`
3. `art-design-pro/src/views/asset/location`
4. `art-design-pro/src/views/asset/info`
5. `art-design-pro/src/views/asset/order`
6. `art-design-pro/src/views/asset/inventory`
7. `art-design-pro/src/views/asset/event`

各目录统一使用：

1. `index.vue`
2. `modules/*`

示意：

1. `src/views/asset/category/index.vue`
2. `src/views/asset/category/modules/category-dialog.vue`
3. `src/views/asset/category/modules/category-search.vue`

## 推荐路由骨架

### 目录级路由

建议资产模块路由结构与现有 `dashboard.ts`、`system.ts` 保持一致：

1. 顶层路径：`/asset`
2. 容器组件：`/index/index`
3. 子路由按业务域拆分

建议路由清单：

1. `/asset/dashboard`
2. `/asset/category`
3. `/asset/location`
4. `/asset/info`
5. `/asset/order`
6. `/asset/inventory`
7. `/asset/event`

### 路由文件建议

建议在 `asset.ts` 里先定义一个完整模块：

1. 顶层名称：`Asset`
2. 一级图标可以沿用业务感较强的图标，如 `ri:archive-line`
3. 子页面统一开启 `keepAlive: true`，但看板可根据性能再决定

### 页面名建议

建议统一命名，避免以后 keepAlive、缓存和埋点混乱：

1. `AssetDashboard`
2. `AssetCategory`
3. `AssetLocation`
4. `AssetInfo`
5. `AssetOrder`
6. `AssetInventory`
7. `AssetEvent`

## 推荐 API 骨架

建议沿用现有 `src/api/system/user.ts` 风格，统一使用：

1. `http.request({ url, method, params/data })`

### `src/api/asset/category.ts`

建议包含：

1. `listCategory`
2. `getCategory`
3. `addCategory`
4. `updateCategory`
5. `delCategory`
6. `treeCategorySelect`

### `src/api/asset/location.ts`

建议包含：

1. `listLocation`
2. `getLocation`
3. `addLocation`
4. `updateLocation`
5. `delLocation`
6. `treeLocationSelect`

### `src/api/asset/info.ts`

建议包含：

1. `listAssetInfo`
2. `getAssetInfo`
3. `addAssetInfo`
4. `updateAssetInfo`
5. `delAssetInfo`
6. `exportAssetInfo`
7. `importAssetInfo`
8. `listAssetInfoEvents`

### `src/api/asset/order.ts`

建议包含：

1. `listAssetOrder`
2. `getAssetOrder`
3. `addAssetOrder`
4. `updateAssetOrder`
5. `delAssetOrder`
6. `submitAssetOrder`
7. `approveAssetOrder`
8. `rejectAssetOrder`
9. `finishAssetOrder`
10. `cancelAssetOrder`

### `src/api/asset/inventory.ts`

建议包含：

1. `listAssetInventory`
2. `getAssetInventory`
3. `addAssetInventory`
4. `updateAssetInventory`
5. `delAssetInventory`
6. `startAssetInventory`
7. `finishAssetInventory`
8. `processInventoryDiff`

### `src/api/asset/event.ts`

建议包含：

1. `listAssetEvent`
2. `getAssetEventByAssetId`
3. `exportAssetEvent`

### `src/api/asset/dashboard.ts`

建议包含：

1. `getAssetDashboardSummary`
2. `getAssetDashboardTodo`
3. `getAssetDashboardTrend`

## 页面骨架建议

## 一期主数据页

### 1. 分类管理页

建议页面结构：

1. 搜索栏
2. 树表或树形列表
3. 新增/编辑弹窗
4. 删除确认

建议模块文件：

1. `modules/category-dialog.vue`
2. `modules/category-search.vue`

### 2. 存放位置页

建议页面结构：

1. 搜索栏
2. 树表或树形列表
3. 新增/编辑弹窗
4. 位置详情可先不单独开页

建议模块文件：

1. `modules/location-dialog.vue`
2. `modules/location-search.vue`

### 3. 资产台账页

建议页面结构：

1. 搜索栏
2. 表格
3. 新增/编辑弹窗
4. 导入弹窗
5. 查看流水抽屉

建议模块文件：

1. `modules/info-dialog.vue`
2. `modules/info-search.vue`
3. `modules/info-import-dialog.vue`
4. `modules/info-event-drawer.vue`

说明：

1. 台账页是前端最重要的中枢页面
2. 这里要尽早把“本人视图 / 部门视图 / 全量视图”的体验差异考虑进去

## 一期流程页

### 4. 业务单据页

建议页面结构：

1. 顶部搜索栏
2. 单据列表表格
3. 右侧详情抽屉或详情弹窗
4. 新建单据弹窗
5. 审批弹窗
6. 状态标签和动作按钮区

建议模块文件：

1. `modules/order-dialog.vue`
2. `modules/order-search.vue`
3. `modules/order-detail-drawer.vue`
4. `modules/order-approve-dialog.vue`

说明：

1. 一期不建议直接上独立详情页路由
2. 先用列表 + 抽屉/弹窗把流程跑通，开发更稳

### 5. 盘点任务页

建议页面结构：

1. 顶部搜索栏
2. 任务列表
3. 任务详情抽屉
4. 创建任务弹窗
5. 差异处理弹窗

建议模块文件：

1. `modules/inventory-dialog.vue`
2. `modules/inventory-search.vue`
3. `modules/inventory-detail-drawer.vue`
4. `modules/inventory-diff-dialog.vue`

### 6. 资产流水页

建议页面结构：

1. 搜索栏
2. 只读表格
3. 导出按钮

建议模块文件：

1. `modules/event-search.vue`

说明：

1. 这个页面一期不需要新增/编辑弹窗
2. 重点是过滤条件和时间线阅读体验

## 一期看板页

### 7. 资产看板页

建议页面结构：

1. 汇总卡片
2. 我的待办
3. 状态分布
4. 近期待处理单据
5. 盘点差异提醒

建议不要一开始做得太重：

1. 先把“有用的待办和异常”做出来
2. 图表少而准，不要为了酷炫堆很多空指标

## 权限接线建议

当前项目已使用：

1. `v-auth="'system:user:add'"`

资产模块建议统一切到完整后端权限标识，不要只写简短动作名。

例如：

1. `v-auth="['asset:info:add']"`
2. `v-auth="['asset:order:approve']"`
3. `v-auth="['asset:inventory:processDiff']"`

不建议前端自己发明：

1. `assetInfoAdd`
2. `approveOrder`
3. `canProcessDiff`

这种自定义 key。

## 推荐页面开发顺序

### 第 1 步

先做标准列表页骨架：

1. 分类
2. 位置
3. 台账

目标：

1. 路由能进
2. API 能通
3. 搜索栏、表格、弹窗风格统一

### 第 2 步

再做流程页骨架：

1. 业务单据
2. 盘点任务

目标：

1. 状态标签统一
2. 动作按钮统一
3. 抽屉/弹窗流程统一

### 第 3 步

最后补只读与聚合页面：

1. 资产流水
2. 资产看板

## 当前不建议做的事

### 1. 不建议现在继续沿用 `asset/ledger`

因为它现在没有形成真正页面骨架，继续沿着这个名字扩展，后面很容易和“资产台账 / 资产流水 / 业务单据”混淆。

### 2. 不建议一开始把单据和盘点做成很多独立详情页路由

一期更稳的方式还是：

1. 一个列表页
2. 若干弹窗
3. 一个详情抽屉

等流程稳定后，再考虑拆独立详情页。

### 3. 不建议页面内写死大量状态文案

资产状态、单据状态、盘点状态最好都走字典或统一映射，避免后面前后端改一个状态要改很多地方。

## 后续建议

如果这份骨架方向没问题，下一步最自然就是继续补：

1. `资产管理系统-开发任务拆分清单.md`

这样我们就可以把文档真正从“设计”推进到“按阶段开工”。
