{
    "method":  "tools/call",
    "params":  {
                   "arguments":  {
                                     "code":  "const raw = localStorage.getItem(\"sys-v3.0.1-user\"); let parsed = null; try { parsed = raw ? JSON.parse(raw) : null } catch (e) {} return { hasRaw: !!raw, parsedType: parsed \u0026\u0026 typeof parsed, parsedKeys: parsed \u0026\u0026 typeof parsed === \"object\" ? Object.keys(parsed) : null, sample: parsed \u0026\u0026 typeof parsed === \"object\" ? { token: parsed.token || parsed.accessToken || null, infoKeys: parsed.info ? Object.keys(parsed.info) : null } : null }",
                                     "timeoutMs":  10000,
                                     "tabId":  1373737751
                                 },
                   "name":  "chrome_javascript"
               },
    "id":  8,
    "jsonrpc":  "2.0"
}
